---
title: 'Health Check'
description: 'Check if the server is running'
---

## GET /health

Returns the server health status. This endpoint does not require the `X-Anima-Namespace` header.

## Response

```json
{
  "status": "ok"
}
```
